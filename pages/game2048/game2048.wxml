<view class="container">
  <view class="header">
    <text class="title">2048</text>
    <view class="score-container">
      <view class="score-box">
        <text class="score-label">得分</text>
        <text class="score-value">{{score}}</text>
      </view>
      <view class="score-box">
        <text class="score-label">最佳</text>
        <text class="score-value">{{bestScore}}</text>
      </view>
    </view>
  </view>
  
  <view class="game-container">
    <view class="game-board" bindtouchstart="onTouchStart" bindtouchend="onTouchEnd">
      <view class="board-row" wx:for="{{board}}" wx:key="index">
        <view class="board-cell cell-{{item || 'empty'}}" wx:for="{{item}}" wx:key="index">
          <text class="cell-text" wx:if="{{item}}">{{item}}</text>
        </view>
      </view>
    </view>
    
    <view class="game-over-overlay" wx:if="{{gameOver}}">
      <view class="game-over-content">
        <text class="game-over-title">游戏结束</text>
        <text class="game-over-score">最终得分: {{score}}</text>
        <button class="restart-btn" bindtap="restart">重新开始</button>
      </view>
    </view>
  </view>
  
  <view class="controls">
    <button class="control-button" bindtap="restart">重新开始</button>
  </view>
  
  <view class="instructions">
    <text class="instruction-text">滑动屏幕移动数字方块</text>
    <text class="instruction-text">相同数字相撞时会合并</text>
    <text class="instruction-text">目标是创造出2048方块！</text>
  </view>
</view>